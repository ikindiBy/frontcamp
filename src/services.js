'use strict'

import  { KEY } from './constants';

const getSourcesByCat = (categoryId) => {
    if (!url || url.slice(0,4) !== 'http') {
        return false;
    } 
    return true;
}

export { getSourcesByCat };